import SingleTableForm from "../../features/SingleTableForm/SingleTableForm";

const WaiterApp = () => {
    return(
        <div>
            <SingleTableForm />
        </div>
    );
};

export default WaiterApp;